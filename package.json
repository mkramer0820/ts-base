{
  "name": "consumevest-client",
  "version": "1.0.0",
  "description": "client to get transaction, auth, publickey",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run check"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.20.0",
    "typescript": "^3.6.4",
    "gts": "^1.1.0",
    "@types/node": "^10.0.3"
  },
  "dependencies": {
    "tslint-config-airbnb": "^5.11.2"
  }
}
